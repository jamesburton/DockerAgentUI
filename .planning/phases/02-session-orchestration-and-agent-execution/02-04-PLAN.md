---
phase: 02-session-orchestration-and-agent-execution
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - src/AgentHub.Orchestration/Coordinator/SessionCoordinator.cs
  - src/AgentHub.Service/Program.cs
  - tests/AgentHub.Tests/SessionCoordinatorTests.cs
  - tests/AgentHub.Tests/SessionHistoryTests.cs
autonomous: true
requirements: [SESS-03, SESS-05]

must_haves:
  truths:
    - "User can stop a session gracefully or force-kill via API"
    - "User can query past session history with pagination"
    - "Session history includes stored output and final outcome (exit code, completion time)"
    - "All new Phase 2 services are wired in DI and API endpoints respond"
    - "Approval resolution has an API endpoint that clients can POST to"
  artifacts:
    - path: "src/AgentHub.Orchestration/Coordinator/SessionCoordinator.cs"
      provides: "Updated coordinator with force-kill and approval integration"
      min_lines: 80
    - path: "src/AgentHub.Service/Program.cs"
      provides: "DI wiring for all Phase 2 services and new API endpoints"
      contains: "ApprovalService"
    - path: "tests/AgentHub.Tests/SessionHistoryTests.cs"
      provides: "Session history query and pagination tests"
      min_lines: 40
    - path: "tests/AgentHub.Tests/SessionCoordinatorTests.cs"
      provides: "Coordinator force-kill and listing tests"
      min_lines: 40
  key_links:
    - from: "src/AgentHub.Orchestration/Coordinator/SessionCoordinator.cs"
      to: "src/AgentHub.Orchestration/Coordinator/ApprovalService.cs"
      via: "Delegates approval checks during input processing"
      pattern: "ApprovalService"
    - from: "src/AgentHub.Orchestration/Coordinator/SessionCoordinator.cs"
      to: "src/AgentHub.Orchestration/Backends/SshBackend.cs"
      via: "Calls StopAsync with forceKill parameter"
      pattern: "StopAsync.*forceKill"
    - from: "src/AgentHub.Service/Program.cs"
      to: "src/AgentHub.Orchestration/Coordinator/ApprovalService.cs"
      via: "DI registration and /api/approvals endpoint"
      pattern: "AddSingleton.*ApprovalService"
    - from: "src/AgentHub.Service/Program.cs"
      to: "src/AgentHub.Orchestration/Monitoring/SessionMonitorService.cs"
      via: "AddHostedService registration"
      pattern: "AddHostedService.*SessionMonitorService"
---

<objective>
Wire all Phase 2 services together: update SessionCoordinator with force-kill support and approval integration, add API endpoints for session history (pagination/filtering), approval resolution, and force-kill. Register all new services in DI.

Purpose: Plans 02 and 03 built the SSH backend and approval/sanitization services independently. This plan connects them through the coordinator and exposes them via API endpoints that CLI and web clients will consume.

Output: Fully wired Phase 2 with all services registered in DI, API endpoints for force-kill, approval resolution, and paginated session history. Integration-ready for Phase 3 CLI and Phase 4 dashboard.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-session-orchestration-and-agent-execution/02-CONTEXT.md
@.planning/phases/02-session-orchestration-and-agent-execution/02-RESEARCH.md
@.planning/phases/02-session-orchestration-and-agent-execution/02-01-SUMMARY.md
@.planning/phases/02-session-orchestration-and-agent-execution/02-02-SUMMARY.md
@.planning/phases/02-session-orchestration-and-agent-execution/02-03-SUMMARY.md

<interfaces>
<!-- Key types from Plans 01-03 that this plan wires together -->

From src/AgentHub.Orchestration/Abstractions.cs (updated in Plan 02):
```csharp
public interface ISessionBackend
{
    Task StopAsync(string sessionId, bool forceKill, CancellationToken ct); // Updated signature
}
public interface ISessionCoordinator
{
    Task StopSessionAsync(string userId, string sessionId, CancellationToken ct); // Needs forceKill param
}
```

From src/AgentHub.Orchestration/Coordinator/ApprovalService.cs (created in Plan 03):
```csharp
public sealed class ApprovalService
{
    Task<ApprovalDecision> RequestApprovalAsync(string sessionId, ApprovalContext context, Func<SessionEvent, Task> emit, CancellationToken ct);
    void ResolveApproval(string approvalId, bool approved, string? resolvedBy);
}
```

From src/AgentHub.Orchestration/Monitoring/SessionMonitorService.cs (created in Plan 02):
```csharp
public sealed class SessionMonitorService : BackgroundService { }
```

From src/AgentHub.Orchestration/Config/ConfigLoader.cs (created in Plan 01):
```csharp
public sealed class ConfigLoader { T Load<T>(string path) where T : class, new(); }
```

From src/AgentHub.Orchestration/Config/ConfigScopeMerger.cs (created in Plan 01):
```csharp
public sealed class ConfigScopeMerger { ScopedPolicyConfig Merge(IReadOnlyList<ScopedPolicyConfig> configs); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update SessionCoordinator with force-kill and approval integration</name>
  <files>
    src/AgentHub.Orchestration/Coordinator/SessionCoordinator.cs,
    src/AgentHub.Orchestration/Abstractions.cs,
    tests/AgentHub.Tests/SessionCoordinatorTests.cs
  </files>
  <behavior>
    - Test: StopSessionAsync with forceKill=false calls backend.StopAsync(sessionId, false, ct)
    - Test: StopSessionAsync with forceKill=true calls backend.StopAsync(sessionId, true, ct)
    - Test: ListSessionsAsync returns sessions from all backends combined, ordered by CreatedUtc descending
    - Test: StartSessionAsync persists prompt from request into the session
    - Test: StartSessionAsync with Prompt=null and Reason="fix bug" uses Reason as fallback prompt
  </behavior>
  <action>
1. Update ISessionCoordinator in Abstractions.cs:
   - Change `StopSessionAsync` signature: `Task StopSessionAsync(string userId, string sessionId, bool forceKill, CancellationToken ct)`

2. Update SessionCoordinator.cs:
   - Add `ApprovalService` to constructor injection
   - Update `StopSessionAsync` to accept and pass `forceKill` parameter to the backend
   - Update `StartSessionAsync`:
     a. Before calling backend.StartAsync, resolve the prompt: use request.Prompt if set, fall back to request.Reason, fall back to empty string
     b. Pass the resolved prompt through to the backend
   - Keep existing sanitization and policy check logic in SendInputAsync
   - Add method: `Task<IReadOnlyList<SessionSummary>> GetSessionHistoryAsync(string userId, int skip, int take, string? stateFilter, CancellationToken ct)` -- query sessions from DB with pagination and optional state filter (for SESS-05 session history). This needs IDbContextFactory injected into the coordinator (or delegate to a dedicated query service).

3. Write SessionCoordinatorTests.cs:
   - Use InMemoryBackend (it's simpler to mock than SshBackend for coordinator-level tests)
   - Update InMemoryBackend.StopAsync to accept forceKill parameter
   - Test StopSessionAsync routes forceKill correctly
   - Test ListSessionsAsync aggregates from multiple backends
   - Test StartSessionAsync prompt resolution logic
  </action>
  <verify>
    <automated>dotnet test tests/AgentHub.Tests --filter "FullyQualifiedName~SessionCoordinatorTests" -x</automated>
  </verify>
  <done>
    - SessionCoordinator.StopSessionAsync accepts and passes forceKill
    - Prompt resolved from request.Prompt or request.Reason
    - Session history query with pagination works
    - All SessionCoordinatorTests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DI wiring, API endpoints, and session history tests</name>
  <files>
    src/AgentHub.Service/Program.cs,
    tests/AgentHub.Tests/SessionHistoryTests.cs
  </files>
  <behavior>
    - Test: GET /api/sessions?skip=0&take=10 returns paginated session list
    - Test: GET /api/sessions?state=Stopped returns only stopped sessions
    - Test: GET /api/sessions/{id}/history returns session events (full output replay for SESS-05)
    - Test: DELETE /api/sessions/{id} with ?force=true sends force-kill
    - Test: DELETE /api/sessions/{id} without force sends graceful stop
    - Test: POST /api/approvals/{id}/resolve with {approved: true} resolves approval
    - Test: POST /api/approvals/{id}/resolve with {approved: false} denies approval
  </behavior>
  <action>
1. Update Program.cs DI registrations:
   - Register ApprovalService as singleton: `builder.Services.AddSingleton<ApprovalService>();`
   - Register SessionMonitorService as hosted service: `builder.Services.AddHostedService<SessionMonitorService>();`
   - Register ConfigLoader as singleton: `builder.Services.AddSingleton<ConfigLoader>();`
   - Register ConfigScopeMerger as singleton: `builder.Services.AddSingleton<ConfigScopeMerger>();`
   - Keep existing SshBackend registration but ensure it uses the new real implementation
   - Add ISshHostConnection interface to DI if needed for testability

2. Add/update API endpoints in Program.cs:
   - `GET /api/sessions` -- add query parameters: `skip` (int, default 0), `take` (int, default 50), `state` (string?, optional filter). Query sessions from DB with pagination. Return JSON array with total count header.
   - `GET /api/sessions/{id}/history` -- return all SessionEvents for the session ordered by timestamp (full output replay per user decision "Full stdout/stderr stored per session as SessionEvents in the DB -- user can replay complete session history")
   - `DELETE /api/sessions/{id}` -- add `?force=true` query parameter. If force=true, call coordinator.StopSessionAsync with forceKill=true. Otherwise graceful stop.
   - `POST /api/approvals/{id}/resolve` -- accept JSON body `{approved: bool, resolvedBy: string?}`. Call approvalService.ResolveApproval(id, approved, resolvedBy). Return 200 on success, 404 if approval not found.
   - `POST /api/sessions` -- update existing endpoint to pass Prompt and IsFireAndForget through to coordinator

3. Write SessionHistoryTests.cs:
   - Use WebApplicationFactory pattern (following SseStreamingTests pattern from Phase 1)
   - Seed sessions and events in DB
   - Test pagination: skip=0 take=2 returns first 2, skip=2 take=2 returns next 2
   - Test state filter: only returns sessions matching requested state
   - Test history endpoint: returns events ordered by timestamp for a specific session
   - Test force-kill endpoint: DELETE with force=true
   - Test approval resolution endpoint: POST approve/deny

4. Run full test suite to ensure no regressions.
  </action>
  <verify>
    <automated>dotnet test tests/AgentHub.Tests --filter "FullyQualifiedName~SessionHistoryTests" -x</automated>
  </verify>
  <done>
    - All Phase 2 services registered in DI
    - GET /api/sessions supports pagination and state filtering
    - GET /api/sessions/{id}/history returns full event replay
    - DELETE /api/sessions/{id} supports graceful and force stop
    - POST /api/approvals/{id}/resolve handles approve/deny
    - SessionMonitorService running as hosted service
    - All SessionHistoryTests pass
    - Full test suite passes
  </done>
</task>

</tasks>

<verification>
```bash
# All new tests pass
dotnet test tests/AgentHub.Tests --filter "FullyQualifiedName~SessionCoordinatorTests|FullyQualifiedName~SessionHistoryTests" -x

# Full test suite passes (no regressions)
dotnet test tests/AgentHub.Tests -x

# Build succeeds
dotnet build AgentSafeEnv.sln --exclude AgentHub.Maui
```
</verification>

<success_criteria>
- SessionCoordinator routes force-kill to backends correctly
- Session history queryable with pagination and state filtering
- Full session output replayable via /api/sessions/{id}/history
- Approval resolution exposed via POST endpoint
- All Phase 2 services registered in DI and running
- Full test suite green (no regressions from Phase 1)
</success_criteria>

<output>
After completion, create `.planning/phases/02-session-orchestration-and-agent-execution/02-04-SUMMARY.md`
</output>
